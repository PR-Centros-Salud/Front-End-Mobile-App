import React from "react";
import { View, Text, Button, TouchableOpacity, Alert, TextInput, StyleSheet, ScrollView} from "react-native";

export default function DoctorList(){
    return(
        <View>
            <Text>LISTA DOCTORES</Text>
        </View>
    );
}
